using UnityEngine;

public class HomingMissile : Boolet
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        SetBullet();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
